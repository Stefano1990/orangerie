<header>
	<div id="top-box">
			<div id="profil-box">
				<% unless user_signed_in? %>
				<%= render 'devise/sessions/new' %>
				<% else %>
					<span>Willkommen zur&uuml;ck <%= current_user.name %></span>
					<%= link_to "Profil bearbeiten", edit_user_path(current_user)%><br/>
					<%= link_to "Zum profil.", user_path(current_user) %><br/>
					<%= link_to "Abmelden", destroy_user_session_path %>
				<% end %>
			</div>
	</div>
  	<nav>
    	<ul>
      		<li><%= link_to "Club", "#" %></li>
			<li><%= link_to "Club3", "#" %></li>
    	</ul>
  	</nav>
</header>
